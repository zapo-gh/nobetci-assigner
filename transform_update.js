    // Transform data to expected format
    const classFree = classFreeRes.data.length > 0 ? classFreeRes.data[0].data || {} : {}

    const teacherFree = teacherFreeRes.data.length > 0 ? teacherFreeRes.data[0].data || {} : {}
